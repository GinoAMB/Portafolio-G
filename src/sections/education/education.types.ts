export interface Education {
  title: string;
  institution: string;
  period: string;
  description?: string;
}

export interface SoftSkill {
  name: string;
}

export interface Certification {
  title: string;
  institution: string;
  year: string;
  pdf: string;
}